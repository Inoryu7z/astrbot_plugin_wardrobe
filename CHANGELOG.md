### v3.0.7

**💬 图片 AI 点评**

* 新增「图片评论」开关，开启后新存入的图片会在空闲时间由模型生成一条可爱的点评，老图不受影响。
* 点评默认在图片存入 1 小时后的空闲时间生成，多图并发时自动排队逐个生成，避免模型压力；延迟时长可配置。
* 点评显示在图片详情中，可单独点进卡片查看。
* 支持手动「重新评论」（右键菜单或详情按钮）：无评论则新增，有评论则覆盖。
* 重新分析后会自动重新评论，确保点评始终基于最新内容。
* 评论模型可独立配置，留空时使用存图主模型；提示词可自定义，默认内置猫娘模板。

---

### v3.0.6

**📝 存图分析提示词可配置**

* 存图分析提示词现在可在插件设置中查看和修改，支持全屏 Markdown 编辑器，便于按需调整分析结构、输出字段与约束强度。
* 留空时自动使用插件内置模板。
* 模板中的 `{pools_text}` 占位符用于注入预定义值池，请勿删除。

---

### v3.0.5

**⚙️ 配置项精简**

* 移除「存图反馈」配置，存图成功后始终静默保存，不再向用户发送图片描述与属性信息。
* 补拍取图的三项参数（候选召回数量、冷梯队容差、冷图配额席位）改为固定值，不再需要单独配置。
* 取图结果始终包含用户保存时添加的标注信息，无需再手动开启。
* 图片大小限制固定为 20MB，超过此大小的图片将被拒绝保存。
* 移除视频提示词模型的直连 API 配置，统一使用已选择的 Provider 模型生成提示词。
* 移除各档位的默认视频后端配置，视频生成时需手动指定后端。

---

### v3.0.4

**⚖️ 补拍取图公平轮换**

* 补拍选参考图时改为按冷梯队顺序直接取用（低热度 → 久未用 → 更贴），不再依赖取图模型挑选，让衣橱里长期吃灰的图有机会被使用，也省一次取图调用。
* 增加补拍候选召回数量，让更多相关但低热度的图片进入候选池。
* 补拍时保证少量"冷图配额席位"进入候选池，即使相似度很低也有机会被用。
* 同一热度下更偏向使用已标记为喜爱的图片。
* 新增配置项：补拍取图公平轮换、补拍候选召回数量、补拍冷梯队容差、补拍冷图配额席位。

---

### v3.0.3

**🧩 部位素材库**

* 新增部位素材库，支持上传局部特写图作为大图生成的局部参考素材。
* 存图模型自动为素材生成短标签与两段式描述（内容简介 + 使用指导），指导大图生成时精准引用。
* WebUI 新增素材管理页，支持上传、编辑备注、删除与预览。
* 素材卡片点击可查看完整详情；编辑支持修改短标签与完整描述。

---

### v3.0.2

**🔧 池子管理修复与重构**

* 修复删除默认池中的风格无效的问题。
* 池子管理 UI 重构：风格池与标签分类分离，人格风格池改为勾选式交互。

---

### v3.0.1

**🔍 取图焦点词召回优化**

* 补拍时带「重点是X。」前缀的描述，现在能更可靠地取到与焦点词匹配的参考图。
* 解决小众品类（如女儿服）因描述变长导致取不到对应标签图的问题。

---

### v3.0.0

**🐛 修复批量导出大文件无法下载**

* 修复图片数量较多时点击导出后浏览器无下载提示的问题。
* 导出图片、导出选中备份、导出全量备份均修复。

---

### v2.9.9

**🔍 存图联网搜索**

* 存图分析新增联网搜索开关，开启后可识别 cosplay 角色、品牌等需要联网查证的内容。
* 支持配置多个提供商，按顺序组成回退链，各自独立设置日限额。
* 配合 token_router 插件实现按日用量自动切换。

---

### v2.9.8

**🎭 批量参考强度编辑**

* 批量标签下拉新增参考强度选项（完整参考/风格参考/重构），可批量为选中图片设置参考强度。

---

### v2.9.7

**🎭 cosplay 标记快捷切换 + 批量打标签**

* 右键菜单新增「切换cosplay标记」，单击即可切换 cosplay 标签。
* 批量标签下拉新增「打上cosplay标签」，可批量为选中图片添加 cosplay 标签。
* 修复右下角批量操作面板背景透明看不清的问题。

---

### v2.9.6

**✨ 右键快捷编辑用户标签 + 修复编辑态方向键切换卡片**

* 卡片右键菜单新增「编辑用户标签」，弹出小气泡可直接输入备注，无需进入详情页；呼出即自动聚焦，回车保存、Esc 取消。
* 修复在详情页编辑模式下，输入框内使用左右方向键会切换上下张图片而非移动光标的问题。

---

### v2.9.5

**🔄 默认风格池更新 + 人格级风格池重置**

* 默认风格池更新。
* 升级后自动清除已配置的人格级自定义风格池，回退为新的默认风格池。

---

### v2.9.4

**🐛 修复按人格热度同步问题 + 备份完善**

* 修复 v2.9.3 引入的图片热度在不同视图间显示不一致的问题。
* 删除图片时同步清理按人格热度记录，避免残留孤儿数据。
* 备份导出/导入现在包含按人格热度数据。
* WebUI 详情页热度字段改为只读（由系统自动维护）。
* 修复 WebUI 重新分析接口未使用人格级风格池的问题。

---

### v2.9.3

**🔄 图片取用热度改为按人格独立计算**

* 人格 A 用过的图，对人格 B 仍显示 0 热度。
* 取图时内容相近的图优先选低热度，避免同一张图被反复返回。
* WebUI 批量操作新增「清除热度」按钮。
* 升级后旧的全局热度自动归零，按人格重新统计。

---

### v2.9.2

**✨ AI 生成图提示词展示**

* 衣橱新增 `ai_prompt` 字段，存储 AiImg 生成该图时所用的提示词。
* 仅自拍模式自动存入时会附带提示词，手动上传的图为空。
* WebUI 详情页新增「AI提示词」字段，为空时自动隐藏。
* 字段可在 WebUI 中编辑修改。

---

### v2.9.1

**🐛 修复 WebUI 无限滚动丢失最后一页图片**

* 修复图片总数不是 24 的倍数时，最后 1~23 张被静默丢弃的问题。
* 现象：删除任意一张图后，原本被藏起来的图会"冒出来"。

---

### v2.9.0

**✨ 人格级风格池（补拍专用）**

* 新增人格级风格池接口，每个人格可有自己的专属风格列表，供 AiImg 补拍选风格使用。
* WebUI 标签分类管理中可编辑每个人格的风格池，支持从全局池快速添加。
* 人格池留空时自动回退到全局风格池。

---

### v2.8.1

**✨ 批量全选 + 筛选导出 + 选择备份导出**

* 批量模式新增「全选」按钮：按当前筛选条件一键全选所有匹配图片。
* 批量模式新增「导出图片」：将选中图片打包为纯图片 ZIP。
* 批量模式新增「导出备份」：将选中图片打包为完整备份 ZIP（含数据库+图片+关联视频），可导入其他服务器。
* 修复备份恢复大文件时的 413 错误和若干视频自动保存 bug。

---

### v2.8.0

**✨ 存储空间优化：视频保留策略、无感模式、批量标签**

* 新增视频保留策略：自动保存的视频超过 7 天后自动删除（WebUI 手动生成的不受影响）。
* 新增无感模式：标记为"无感"的图片超过 30 天后自动彻底删除。无标签图片不会被自动清理。
* 新增自动备份开关，可关闭以节省存储空间。
* 新增批量标签操作：批量模式下可一键将选中图片标记为收藏/喜欢/无感/取消。

---

### v2.7.0

**♻️ 备份系统重构**

* 自动备份从每日全量改为**每月全量 + 每日增量**，大幅降低日常备份压力。
* 保留 30 天内备份，始终保留最新全量备份。
* WebUI 恢复支持多文件上传，自动识别全量/增量并按时间顺序恢复。

---

### v2.6.5

**✨ 补拍衰减过滤机制**

* 补拍近期用过的参考图权重降低，避免短期内重复使用同一张图。
* 每周一凌晨所有图的补拍计数减 1，长期不用自动恢复权重。
* 仅影响补拍场景，手动自拍和手动取图不受影响。

---

### v2.6.4

**🐛 修复视频发送体验问题**

* 修复视频上传超时被误判为失败、导致用户收到重复视频的问题。
* 新增 30 秒宽限期，等待协议端后台上传完成。
* WebUI 视频发送按钮现在区分「发送成功」「上传中」「发送失败」三种状态。

---

### v2.6.3

**🐛 修复视频发送失败**

* 修复视频文件 faststart 处理损坏文件、导致协议端上传终止的问题。
* 修复 Windows 路径下视频发送失败。
* 所有发送方式均失败时，发送视频链接作为纯文本兜底。

---

### v2.6.2

**🐛 视频功能数据完整性 + 性能优化**

* 修复删除图片时未清理关联视频，导致视频成为孤儿数据。
* 修复备份完全不包含视频数据，现在导出/导入包含完整视频记录与文件。
* 视频文件端点新增缓存头，重复播放不再重新下载。
* 视频下载改为流式分块，大视频不再内存溢出。

---

### v2.6.1

**🐛 修复 v2.6.0 引入的若干问题**

* 修复视频 faststart 误判导致不必要的视频重写。
* 修复随机排序池耗尽后滚动哨兵仍可见的问题。
* 后台任务保留引用，防止被垃圾回收。

---

### v2.6.0

**✨ 视频发送到会话 + 随机排序**

* WebUI 视频设置中新增「视频发送会话」配置，填写后可将视频发送到指定会话。
* 视频生成面板新增「生成后自动发送」选项；播放器弹窗新增「发送视频」按钮。
* 图片排序新增「随机」选项。

---

### v2.5.9

**🐛 修复 PC 端无限滚动重复加载**

* 修复批量上传图片排序不确定导致的分页重复。
* 修复加载失败时状态未重置导致无限滚动永久卡死。

---

### v2.5.8

**🐛 修复移动端/平板端瀑布流布局错位**

* 移动端改为双列真瀑布流，图片不再挤成一条竖线。
* 平板端间距不均问题修复。

---

### v2.5.7

**🔧 新增视频保存接口供外部插件调用**

* 新增接口接受视频字节流，不依赖事件对象。
* 修复 DailySharing 等插件主动发视频时无法自动保存的问题。

---

### v2.5.6

**🚀 视频流式传输 + faststart 秒开**

* 视频文件改为流式传输，30~40MB 视频不再一次性读入内存。
* 支持浏览器 Range 请求，视频可边下边播。
* 新增 MP4 faststart 后处理，浏览器首次请求即可开始播放。

---

### v2.5.5

**🔧 视频重试复用提示词**

* 重试视频时复用已有提示词，不再重复调用 LLM，节省 token 并加快重试速度。

---

### v2.5.4

**🚀 视频库加载加速**

* 数据库新增轻量查询，减少不必要数据传输。
* 前端视频库改为并行请求 + 客户端缓存，二次切换秒开。
* 视频卡片渲染改为程序化构建，消除 XSS 风险并提升性能。

---

### v2.5.3

**🌟 无人格筛选**

* 图片库和视频库新增「无人格」筛选选项，可筛选未绑定人格的图片/视频。
* 语义检索也适用无人格筛选。

---

### v2.5.2

**🌟 自动存视频**

* AiImg 生成的视频和 DailySharing 分享的视频自动存入衣橱视频库。
* 自动入库的视频继承源图片关联，MD5 去重。
* `/衣柜统计` 现在同时显示视频数量。

---

### v2.5.1

**🐛 视频提示词修复 + WebUI 视频库完善**

* 修复视频提示词模型没收到图片、产出全为幻觉的问题。
* 新增直连 API 方式调用 Vision 模型，绕过 Provider 传图不可靠问题。
* 视频提示词输出改为 JSON 格式，分离思考过程与最终提示词。
* WebUI 视频库界面与图片库统一，播放器支持左右箭头快速切换。
* 失败视频支持重试和删除，重试复用原提示词。

---

### v2.5.0

**✨ 图片转视频（WebUI）**

* 新增图片转视频独立板块，可为衣橱中任意图片生成视频。
* 支持三档视频风格：正常、轻荤、重荤，每档可独立绑定不同的视频后端（复用 AiImg 服务商）。
* Vision 模型自动生成提示词，系统提示词可在 WebUI 中自定义。
* 视频后台异步生成，状态实时更新。
* 视频文件支持流式播放。

---

### v2.4.1

**🔧 参考图接口新增相似度阈值参数**

* 参考图搜索新增 `min_similarity` 参数，可按需收紧搜图条件。
* 不传时使用全局阈值，不影响日常取图。

---

### v2.4.0

**🔧 向量检索优先 + 索引扩展**

* 向量检索可用时跳过意图解析模型，直接用自然语言做语义搜索，减少一次 LLM 调用。
* 向量索引新增风格和服装类型字段，解决按风格搜图找不到的问题。
* 意图解析模型降级为回退路径。

---

### v2.3.9

**🔧 任务间隔拉长**

* 旧图重分析、参考强度回填的任务间隔从 2s 拉长到 30s，减轻 API 并发压力。

---

### v2.3.8

**✨ 全屏大图左右箭头**

* lightbox 支持 ← → 切换上一张/下一张，支持键盘导航，显示图片序号。
* 打开全屏大图时当前图片优先加载。

---

### v2.3.7

**✨ 瀑布流布局 + 详情页箭头导航**

* 图片按原始宽高比展示，不再裁成统一正方形。紧凑模式 5 列，大图模式 3 列，移动端自适应。
* 详情页支持箭头键快速切换上下张。
* 移除手动"加载更多"，改为滑到底部自动加载。

---

### v2.3.5

**✨ 缩略图预生成 + 右键菜单 + 视图切换**

* 存图时自动生成缩略图，浏览时直接返回缓存。
* 图片卡片支持右键快捷操作（收藏/喜欢/删除/重新分析/切换参考强度）。
* 工具栏新增紧凑/大图两种浏览模式，偏好自动记忆。

---

### v2.3.4

**⚡ 性能优化**

* 已加载原图的卡片保留渲染内容，滚动回来不再重新解码。
* 图片添加异步解码，快速滑动更流畅。

---

### v2.3.3

**⚡ WebUI 性能优化**

* 图片端点添加 HTTP 缓存头 + ETag，二次访问直接使用缓存。
* 原图懒加载：仅加载视口内可见卡片，带宽占用降低 70%+。
* CSS 跳过离屏卡片渲染，多图不再卡顿。
* 启动时后台批量生成缺失缩略图。

---

### v2.3.2

**✨ 缩略图系统 + 批量重分析失败图**

* WebUI 图片卡片先加载缩略图，后台自动加载原图替换，大幅提升首屏速度。
* 批量操作栏新增「重分析失败图」按钮。
* 详情弹窗支持左右箭头导航。

---

### v2.3.0

**🐛 参考图人格搜索策略修复**

* 修复 AiImg 参考图搜索时忽略 `search_persona_mode` 配置的 bug。
* `search_persona_mode` 配置项更名为"参考图人格搜索策略"，明确仅影响 AiImg 参考图搜索。
* Bot 自己取图时不再错误应用参考图搜索策略。

---

### v2.2.9

**🐛 修复无人格筛选不生效**

* 修复 `persona=""` 被当作"不过滤人格"处理，导致实际返回了所有人格的图。

---

### v2.2.8

**✨ 最近调用排序 + 自动存图不阻塞**

* WebUI 新增「最近调用」排序：按图片最后被调用的时间倒序排列。
* 图片卡片显示最近调用时间。
* 修复自动存图阻塞角色回复的问题，改为后台异步执行。

---

### v2.2.7

**✨ WebUI 统计分析可视化 + 配置项**

* 新增统计分析页面：景别分布饼图、氛围环形图、风格/场景 Treemap，点击图表可跳转图片视图。
* 新增存图趋势折线图。
* 新增「取图结果包含用户标注」配置项，开启后取图返回描述中会包含用户保存时添加的标注。
* 批量操作栏改为顶部固定定位。

---

### v2.2.6

**🔧 取图策略优化**

* 取图人格搜索策略更名：`exclude_all` → `no_persona_only`，`self_first` → `fallback_other`。
* 取图选择策略优先级调整：服装与姿势表述提升为最高优先级。
* 取图匹配策略放宽：不完全不匹配就返回，宁可多返回不漏掉。

---

### v2.2.5

**✨ 自动备份**

* 每日凌晨自动备份衣橱数据（数据库记录 + 图片文件）。
* 可通过 WebUI 导入恢复。

> ℹ️ 此版本的备份策略已在 v2.7.0 中重构为每月全量 + 每日增量。

---

### v2.2.4

**🐛 多项 Bug 修复 + 参考强度完善**

* 修复删除图片时未清理向量索引。
* 修复重分析时未更新参考强度理由字段。
* 修复 WebUI 编辑/切换参考强度后卡片不实时更新。
* 新增 `ref_strength_reason` 字段：模型分析时输出评级理由，仅在日志和 WebUI 可见，取图时屏蔽。
* 参考强度按钮移至详情弹窗底部操作栏，面板式三档选择。
* 卡片显示参考强度标注。

---

### v2.2.3

**✨ 参考强度字段**

* 新增 `ref_strength` 字段：存图时自动评估姿势与构图的参考价值（三档：full/style/reimagine）。
* 评估标准与服装美观程度完全解耦，避免因服装好看而给高姿势评分。
* 参考图接口返回值新增参考强度字段。

---

### v2.2.2

**✨ Rerank 重排序模型**

* 向量检索后可选 Rerank 精排，进一步提升搜索精度。
* 新增 `rerank_provider_id`、`rerank_top_k`、`rerank_min_candidates` 三个配置项。

---

### v2.2.1

**✨ 优先低热度配置 + WebUI 修复**

* 新增「优先低热度」配置项，开启后取图时优先返回使用次数少的图片。
* 修复 WebUI 404 日志噪音问题。

---

### v2.2.0

**✨ 向量检索相似度分数**

* 向量检索结果现在包含相似度分数，结果按相似度从高到低排序。

---

### v2.1.9

**🐛 WebUI 热重载修复**

* 修复插件重载后 WebUI 不重启的问题。
* `search_persona_mode` 配置改为下拉选择。

---

### v2.1.8

**🔧 相似度阈值可配置**

* 向量检索相似度阈值改为可配置项 `vector_search_min_similarity`（默认 0.5）。

---

### v2.1.7

**🐛 修复向量检索返回不相关结果**

* 添加相似度阈值过滤（默认 0.5），低于阈值的结果会被过滤并回退到 LIKE 搜索。

---

### v2.1.6

**🐛 向量检索去重 + LIKE 回退增强**

* 修复向量检索结果重复：重启后自动重建索引映射并清理重复条目。
* LIKE 回退策略增强：新增渐进式前缀截断和字符级 AND 匹配。

---

### v2.1.5

**✨ 取图人格搜索策略 + 批量操作修复**

* 新增取图人格搜索策略配置：`exclude_all`（默认，优先搜无人格图）或 `self_first`（先搜当前人格）。
* 修复批量操作面板按钮无效。
* 修复 WebUI 搜索不使用向量检索。
* LIKE 回退策略优化：完整关键词搜不到时自动 bigram 分解搜索。

---

### v2.1.4

**🐛 批量上传/重新分析修复**

* 修复批量上传/重新分析的间隔逻辑，改为每 5 秒发起下一个请求，不等上一个完成。
* 修复批量上传中新图片不可见，每张上传成功后立即刷新网格。

---

### v2.1.3

**✨ 批量操作进度面板 + 失败重试**

* 批量上传/重新分析新增右下角浮动进度面板，实时显示逐张进度。
* 失败项支持单张重试和一键重试全部。
* 修复登录后必须刷新才能进入的问题。

---

### v2.1.2

**🐛 批量重新分析修复**

* 修复批量重新分析 API 异常时无日志无响应。
* WebUI 重新分析添加完整日志。

---

### v2.1.1

**✨ 批量重新分析 + 批量上传非阻塞**

* WebUI 批量模式新增「重新分析」按钮，支持批量选中图片后一键重新分析。
* 批量上传点击后弹窗自动关闭，上传在后台继续，用户可继续浏览 WebUI。
* 批量上传进度指示器实时显示上传进度。

---

### v2.1.0

**✨ MD5 去重 + 喜爱优先排序**

* 存图时自动计算 MD5 哈希，检测到完全相同的图片时跳过保存并提示。
* 启动时自动扫描旧图，回填 MD5 哈希值。
* WebUI 排序新增「喜爱优先」选项。

---

### v2.0.0

**✨ 精细化检索字段 + 收藏/喜欢机制**

* 新增多个精细化检索字段（画面聚焦、魅力感、特征标识、道具等），大幅提升搜索精准度。
* 收藏/喜欢双层标记机制：取图时优先返回收藏图片。
* 图片热度机制：取图/参考图时自动计数，支持按热度排序。
* WebUI 批量上传 + 详情页全面改造，所有字段可查看和编辑。
* 暴露程度分级细化。
* 旧图自动重分析：检测新字段为空时逐张重分析。

---

### v1.8.1

**✨ 向量语义检索**

* 新增向量语义检索：基于 Embedding 模型，支持自然语言语义匹配，向量模型失效时自动回退到关键词匹配。
* 向量检索解决相似描述的精准匹配问题（如区分"中华风甜系"和"蓝白蕾丝拼接"）。
* 新增 `embedding_provider_id` 配置项，允许指定专用 Embedding 模型。

---

### v1.7.0

**✨ 备份导出/导入 + 性能优化**

* WebUI 一键导出所有图片元数据和图片文件为 zip 包，方便迁移到新服务器。
* 上传 zip 备份文件一键恢复，已有数据按 ID 跳过不会被覆盖。
* 图片列表改用轻量 API + "加载更多"模式，首屏渲染更快。
* 图片详情按需加载。
* 上传限制提升至 512MB。

---

### v1.6.8

**🐛 WebUI 修复 + 搜索优化**

* 修复 WebUI 端口占用问题，切换 ASGI 服务器为 uvicorn。
* 修复关键词搜索遗漏 user_tags 字段。
* 修复搜索匹配失败时不回退原始查询文本的问题。
* 修复 category 过滤过于严格的问题。

---

### v1.6.7

**🐛 人格丢失修复**

* 修复取图/存图时 LLM 工具调用不传 persona 参数导致搜索走全局的问题。
* 现在当 persona 为空时自动从对话上下文获取当前人格名。

---

### v1.6.6

**🐛 WebUI 上传修复**

* 修复 WebUI 上传图片报"网络错误"。
* 修复前端上传失败时显示"网络错误"而非真实错误信息。
* 提升上传限制到 64MB，超时提升到 300 秒。
* `/存图` 命令支持双参数：`/存图 人格名 描述`。

---

### v1.6.5

**🐛 自动存图修复**

* 修复自动存图不区分生成模式的问题，现在仅保存自拍模式生成的图片。

---

### v1.6.4

**🔧 人格获取修复**

* 修复人格获取失败问题，增加 `persona_manager` 回退逻辑。
* 修复 `/自拍` 命令不自动存图问题，新增 `after_message_sent` 钩子捕获命令方式生成的图片。

---

### v1.6.3

**🔧 自动存图配置简化**

* 简化自动存图配置，删除冗余配置项，直接使用当前对话人格。
* WebUI 详情弹窗新增用户标签字段显示。

---

### v1.6.2

**🔧 WebUI 端口占用 + 错误处理**

* WebUI 端口被占用时自动尝试递增端口（最多 10 个）。
* API 错误处理增强，防止损坏数据导致 500 错误。
* 数据库初始化优化，避免每次请求重复执行。

---

### v1.6.1

**🔧 配置界面优化**

* 人格配置改为列表式界面，每个人格独立配置规范名和别名。
* 多人格自动存图：新增开关，自动存图可跟随当前对话人格。
* 向后兼容旧版配置。

---

### v1.6.0

**✨ AiImg 双向联动**

* 新增参考图接口，供 AiImg 插件在生图时调用获取参考图。搜索时排除当前人格的图库，避免同质化。
* 自动存图：监听 AiImg 工具调用，自动将生成的图片存入衣柜库。
* 配置项命名统一。

---

### v1.5.0

**✨ WebUI 管理界面 + 人格子目录**

* 全新 Web 管理界面，支持图片浏览、搜索、上传、删除等管理。
* 图片网格浏览、关键词搜索、批量操作、图片详情弹窗。
* 简单密码认证，默认端口 18921。
* 存图时支持指定人格名，图片归入对应人格目录。
* 取图时模型自动判断是否按人格过滤。

---

### v1.0.0

**👗 首次发布：图片衣柜管理插件**

* 智能存图：视觉模型自动分析图片内容，生成结构化属性标签。
* 语义检索：自然语言检索图片，取图模型解析意图并匹配。
* LLM 工具注册：`save_wardrobe_image` 和 `search_wardrobe_image`。
* 双模型配置与 Fallback：存图模型和取图模型分别配置，支持主备切换。
* 管理指令：`/存图`、`/删图`、`/衣柜统计`。
